# hxy-diy 当前状态

更新时间：2026-09-23（Asia/Shanghai）

本文件只记录三个开发窗口开始任务时需要知道的当前事实。发布和回滚历史保存在 `docs/WORK-STATUS.md`。

## 当前生产

- 顾客端服务位多人共享未提交选单已上线。业务 PR #138 合并为 `f80b633843dbc12eb5e0340ce552c435c725de53`；该提交的首轮主干 CI `35842678289` 仅因两份设计文档尾部空行触发 Static contracts 失败，PR #139 修正后主干 `77b4c818605fc0533e56d76ccaef27ce5d0abf15` 的 CI `35842988929` 全量成功。功能随包含上述提交的 `314f7eec241f2dd0f59914e586d163007b1f846b` 通过生产部署 `35843520748` 上线，并继续包含在当前更晚 release 中。公网 OpenAPI 与顾客 JS 已核实包含协作者令牌、版本冲突和共享提示。
- 生产最小浏览器验收使用当时空闲的 1 号沙发签名码：两台隔离浏览器加入同一草稿且令牌不同，第二台增选后第一台自动同步；陈旧版本写入返回 409 `CART_VERSION_CONFLICT` 且未覆盖；扫码首屏为菜单，匿名「评价与建议」只打开独立到店反馈。测试未提交服务单，草稿已清空，但未使用管理后台强制释放其空占用。提交后原子锁定、跨床隔离、本人服务评价权限仍以合同/集成测试为证据；微信真机、弱网、门店多人同时操作及真实完成服务评价尚未现场验收，不得标记为 `Field Accepted`。

- 品牌总部菜单已改为显式允许清单：PR #146 合并为主干 `7125930c9c3d3fd78875498e1ae67954dc609604`，主干 CI `35850024744`、显式生产部署 `35850385797` 的备份、恢复演练、部署与验证作业成功。总部仅显示有总部接口路径的审计日志、服务项目、项目加项、商城商品、员工账号和门店主数据；技师管理等需门店上下文的页面不显示，保留旧地址也会转回总部默认页。管理端 216 项测试与构建通过，公网健康接口 production / ok，`/admin/` 和本次 JS `/admin/assets/index-C_gCqg0U.js` 均 HTTP 200。真实账号分别进入总部/门店、门店二维码逐位管理和打印仍待人工验收。

- 品牌总部保留旧门店地址的路由守卫已随 PR #144 的主干 `390eb2b2028bede0211ff709257df5e2f18152cb` 发布；主干 CI `35847716336`、显式生产部署 `35848074456` 的备份、恢复演练、部署与验证作业成功。总部未绑定门店时，`/today` 等门店运营页不再出现在导航，保留的旧地址会在页面挂载前转回总部默认页；门店工作区可继续访问。管理端 215 项测试与构建通过，公网健康接口 production / ok，`/admin/` 与本次 JS `/admin/assets/index-DpYwrnS8.js` 均 HTTP 200。真实浏览器旧地址跳转仍待人工验收。

- 品牌总部工作区默认落点已修复：PR #142 合并为主干 `33f1eaf6c376b2bd9770aff6bb4850a1fa7772c0`，主干 CI `35845807723`、显式生产部署 `35846236622` 的备份、恢复演练、部署与验证作业成功。总部管理员未绑定门店是正常权限边界；此前默认进入门店专属“今日运营”，导致两个接口返回“当前账号未绑定门店”。现在总部默认进入“门店主数据”，绑定门店的店长仍进入“今日运营”。本地 214 项管理端测试与生产构建通过；公网健康接口 production / ok，`/admin/` 与本次 JS `/admin/assets/index-U9-K8EWA.js` 均 HTTP 200。真实浏览器重新登录后的跳转和门店列表仍待人工验收。

- 管理后台工作区选择误报“结果不匹配”的前端修复已随 PR #140 的主干 `314f7eec241f2dd0f59914e586d163007b1f846b` 发布；主干 CI `35843262747`、显式生产部署 `35843520748` 的备份、恢复演练、部署与验证作业成功。后端返回 `brand_admin` / `store_manager` 等工作区角色，前端现在按原始角色校验，再转换为现有菜单使用的 `admin` / `manager` 会话角色；单工作区直登也完成转换。本地管理端 213 项测试及生产构建通过。公网健康接口为 production / ok，`/admin/` 与本次构建的 `/admin/assets/index-BlQvvRoo.js` 均 HTTP 200；真实浏览器重新登录总部、门店仍待人工验收。

- 管理后台多工作区选择页已随 PR #136 的主干 `ff3f4675810b9910d1351e9a44413899a0fc9c35` 发布；主干 CI `35838800537`、手动生产部署 `35839162357` 成功，该次发布时服务器 `current` 为 `github-ff3f4675810b-35839162357`。`admin` 原有的 1 号店 `store_manager` 授权 ID 4 已在发布后恢复为 active，品牌管理员授权 ID 1 仍 active；当时凭证版本由 4 增至 5，系统审计 ID 1157 记录恢复原因。线上后台脚本包含工作区选择界面，后台和健康接口 HTTP 200；真实账号分别进入总部、门店及店长二维码管理页面仍待人工验收。

- 到店反馈、顾客端菜单同步及连锁品牌授权底座已随主干 `34fab31cbcb8d3a27801fe81bd6539a93233c241` 发布。业务 PR #130、#132、#133 与迁移许可 PR #134 均已合并；主干 CI `35833984523`、生产部署 `35834287369` 成功。服务器 `current` 为 `github-34fab31cbcb8-35834287369`，发布前备份 `pre-github-34fab31cbcb8-35834287369-20260923T080016Z.dump` 的 SHA-256 校验通过；隔离恢复升级和生产升级完成，数据库 revision 为 `20260921_staff_scope`，`visit_feedback` 与 `staff_scope_assignments` 表存在。
- 该次发布后曾因前端缺少多工作区选择页临时停用 1 号店店长授权；现在已按上方最新记录恢复。生产库已有 8 个活动沙发服务位和对应活动二维码记录，但门店尚未张贴二维码；真实微信扫码、匿名/登录提交、跨店可见范围、弱网与重复提交仍待门店现场验收，不得宣称 `Field Accepted`。

- 技师服务后记录已重构为可交接的极简记录：沟通偏好、最多三组“身体部位 + 下次怎么做”、本次实际调整为主；年龄段、性别和仅本人可见备注按需补充。v1-v6 历史记录继续兼容，管理端不展示身体细节、年龄、性别或私人备注。
- PR #128 合并业务实现，PR #129 修复文档门禁；该次发布主干为 `cfa957c512573330db60016eca7caf13898e6332`。完整主干 CI `35590967031` 成功并生成双端制品，生产部署 `35591215199` 的资格校验和“Backup, rehearse, deploy and verify”作业均成功。
- 发布后独立公网核验：`/`、`/admin/`、`/technician/`、`/api/v1/health` 均为 HTTP 200。微信真机、弱网、重复提交及门店营业现场未验收，不能以本次自动化与公网探针替代。

- 1 号店沙发显示号已调整为左侧 `1、2、3、5`、右侧 `6、7、8、9`。PR #125 合并为主干 `9b60d3a3b6f60ff2cef161e396d29c718cd3bd77`，主干 CI `35579488848` 成功。
- 该次平面图更新时完整 release 为 `github-c76ac301efa0-35576431361`；顾客端使用该生产基线叠加静态构建 `421212a`。数据库仅更新 `sofa-04` 至 `sofa-08` 的 `name/customer_label`，稳定编码 `sofa-01` 至 `sofa-08`、二维码、坐标、排序、占用和历史关联未改。操作前备份为 `pre-sofa-renumber-20260921T084829Z.dump`。
- 发布后公开服务位地图返回 `1、2、3、5、6、7、8、9`，坐标对应左四右四；首页引用 `/assets/index-CZ7oTNho.js`，健康接口返回 `ok`。微信真机的最终视觉排列仍待现场复核。

- 最新已核验发布：`c76ac301efa07f803d251453a5c99151b4a62dc4`（PR #123，商品目录迁移发布许可；业务实现来自 PR #121）。主干 CI `35575952327` 成功；显式生产部署 `35576431361` 的资格校验及“Backup, rehearse, deploy and verify”作业均成功。
- 生产 release 为 `github-c76ac301efa0-35576431361`，发布前备份为 `pre-github-c76ac301efa0-35576431361-20260921T081716Z.dump`，校验通过；恢复演练库与生产库均成功升级到 Alembic `20260921_product_catalog`，服务器 `current` 已指向该 release。
- 发布后独立公网核验：`/`、`/admin/`、`/technician/`、`/api/v1/health` 均为 HTTP 200，健康接口返回 production / ok。1 号店公开商品目录返回 6 条，`member_price_cents`、`display_order`、`detail_modules` 字段齐全，顺序符合 `display_order` 升序后再按商品 ID 升序。总部/店长真实账号写入、跨店权限和门店营业现场仍待验收。

- 顾客端茶饮已更新为菊花清润茶、薏米祛湿茶、桑葚滋养茶，并配套三张荷小悦 IP 品牌杯图片。该次业务变更由 PR #113 合并为 `c3919ff`；图片路径修复由 PR #114 合并为 `285a233`。
- 该次发布时服务器 `current` 为 `/root/hxy-diy-20260811/releases/manual-main-c3919ff`；随后按静态资源热更新方式写入 `285a233` 的顾客端构建。公网首页引用 `/assets/index-BKhw3lzL.js`，三张 WebP 均 HTTP 200，页面内容接口返回三款新茶饮，健康接口为 `ok`。当前 `current` 以上方最新 release 为准。
- 本次未改数据库、价格、状态机、权限或门店隔离。公网校验不替代微信真机的茶饮卡片视觉与缓存刷新验收。

- 最新已核验发布：`f6ab1f98c91cdef4981885aaf7627a5b03566ab9`（PR #110，技师今日看板服务单计数澄清）。主干 CI `35199237392` 与显式生产部署 `35201013678` 均成功；部署中的“Backup, rehearse, deploy and verify”作业成功完成。公网 `/technician/` 与 `/api/v1/health` 均为 HTTP 200，健康接口返回 production / ok。
- 今日看板将服务中、待服务、待核对和已完成服务单分开显示；已完成但顾客仍在位的记录不会再被称为“当前服务单”。本次仅调整技师端呈现，未改数据库、API、价格、服务位状态机、权限、门店隔离、幂等或审计。
- 该发布不替代微信真机、弱网/重复点击或营业现场验收；新数量文案尚待在真实门店设备上复验。

- 最新已核验发布：`3484080a900947c59456a4c272d1bc7ff0317fd6`（PR #107，且包含 PR #106 的上次服务调整展示）。主干 CI `35081036887` 成功；显式触发部署 `35081465247` 的“Backup, rehearse, deploy and verify”成功完成。公网 `/`、`/admin/` 与 `/api/v1/health` 均为 HTTP 200。
- 技师服务连续性闭环已上线：默认服务后记录只包含服务方式、实际调整、反馈/下次安排，身体情况仍按需补充；“查看上次”展示安全摘要与上次实际调整。v5 记录仅创建技师可在本人历史中追加一次带原因的更正，原记录保留；管理端与其他技师不获得私人文字或身体细节。无数据库迁移、价格、服务位状态机或智慧宝物理资源变更。
- 2026-09-16 已在生产的“模拟技师-P0验收”测试门店完成浏览器闭环验收：匿名选单提交后，测试技师登录即看到待确认服务单；完成服务后可用“本次无补充”收尾，并在本人历史看到结果。一次带原因的更正可将经顾客确认的温度、沟通、反馈和下次安排呈现为精简摘要；未写入身体信息或私人笔记。同一浏览器的第二次匿名选单未继承上次参考，符合不以设备识别自然人的边界。两张测试服务单均已完成，浏览器会话已退出。
- 此验收不替代微信真机、摄像头、弱网/重复提交和门店真实营业验收；待按 `docs/operations/technician-body-map-field-acceptance.md` 补齐这些外部环境场景及跨角色可见范围复核。

- 最新已核验发布：`2f9566e3e8e2ae85412cf70a1d5d1cd76b8d1732`（PR #95）。主干 CI `34820540269` 成功；显式触发部署 `34823261580`，发布作业成功，服务器 `current` 为 `github-2f9566e3e8e2-34823261580`，Manifest 校验通过。顾客端五方卡片、五张药材插画、OFL 圆体与加购顺序已上线。
- 1 号店现煮草本泡已启用目录 ID 9 / version 3：仅替换免费草本方，顺序木火土金水。原草稿与发布目录逐字段一致后才复用；其他选项、价格和历史目录不改。通过事务回滚预演后发布并落系统审计，未冒用员工身份。未执行数据库结构迁移。
- 首页、新 CSS/JS、五张 WebP 和字体探针 HTTP 200，健康接口 `status: ok`；实际生产浏览器确认五方显示、水方切换与图片加载。微信真机及现场下单/技师接收仍待验收。下列版本是历史发布证据，不代表最新 current。

- 最新已核验发布：`efb8c187fdb280f90cad159e81d03dda8cd22651`（包含年度会员闭环、媒体引用保护、已支付项目销量与门店员工账号管理）。GitHub 手动发布任务 `34810436752` 由维护者发起并成功完成；服务器 `current` 已切换至 `github-efb8c187fdb2-34810436752`。公网 `/`、`/admin/`、`/technician/` 与 `/api/v1/health` 均为 HTTP 200。该证据不替代会员换机、赠送权益核销、员工调岗、媒体解除引用删除或门店真实营业流程的人工现场验收。
- 已核验发布：`d7ca1f78341b1108746a2d0f62bb29297b4a3eb2`（PR #67，顾客评价与项目分享）。GitHub 可信 CI `34366546055` 和部署 `34366753130` 均成功；服务器 `current` 已切换至 `github-d7ca1f78341b-34366753130`。
- 顾客评价改为必须显式评分，按评分展示分层标签且最多三项，文字评价最多 300 字并提示隐私边界；评价成功后可回到本人到店记录或再次选购。低分自动回访未实现，也未承诺。
- 项目详情分享链接进入公开只读的 Open Graph 落地页，返回项目标题、简介和已发布主图缩略图，再跳转项目详情；不携带服务位、二维码、选单或身份凭证。线上分享页、缩略图、首页与 `/api/v1/health` 均为 HTTP 200；真机微信聊天卡片展示仍待验收。
- 本次没有数据库迁移、价格规则、服务位状态机或既有顾客业务 API 变更。
- 此前已核验发布：`cae3f208c68c37247e1d9f477f58970290bfe6d8`（PR #66，服务笔记重设计）。CI `34359213054`、部署 `34359434662` 均成功，报告 `deployment_succeeded`（2026-09-09 22:10 Asia/Shanghai）。以下较早发布条目为历史证据，不代表最新版本。
- 服务笔记直接填写、常用要求展开；身体补充具名选部位后同页填写，退出保护及清空修复。沿用 v5 契约，无数据库迁移或权限扩张。
- 公网技师入口已引用新版资源，脚本包含“记住这次，下次更好”和“完成身体补充”，样式包含 `technician-notebook`；健康接口返回 production / ok。未重新读取服务器 current 链接；微信真机及门店闭环仍待验收。

- 主干已发布提交：`85c8f46410bb4705b1c60332772d934d9846a356`（PR #63，技师服务后极简快记）。GitHub 可信 CI `34304218505` 与生产部署 `34304347004` 均为 `success`，发布报告为 `deployment_succeeded`。
- 本次只调整技师端前端交互：默认快记收敛为沟通、关键调整、结果与一句交接；身体记录先选大部位，按需进入精确点位。没有数据库迁移、API 载荷、权限或隐私边界变化。
- 发布后只读公网核验：`/technician/`、`/api/v1/health` 均为 HTTP 200，线上入口的脚本含“交接一句话”和“先选常用部位”。未执行登录写入或门店真实服务；真机微信与营业现场验收仍待完成。
- 主干已发布提交：`f2f893797011aea1f9aa924edf2686c5b18af8f8`（PR #51，已完成服务单不再展示不适用的“查看上次服务参考”入口）。
- GitHub 可信 CI `34126497735` 与生产部署 `34126720373` 均为 `success`；授权测试技师账号在生产页面复验：已完成服务单仅显示“填写服务参考”，不再触发原先会返回 409 的读取请求。该复验不替代真实微信或门店现场验收。
- 本次只改技师端前端展示条件，不涉及数据库迁移、画像字段或服务端权限边界。
- 主干已发布提交：`4b2764331c86d9dae02452d748d5f08dc1c5b402`（PR #49，技师人体标注服务记录）。
- GitHub 可信 CI `34116640565` 与生产部署 `34116796271` 均为 `success`；发布报告状态为 `deployment_succeeded`。
- 本次没有数据库迁移；v1–v4 历史服务参考保持只读兼容。
- 以上是 CI/部署任务证据，不替代生产环境当前身份核验或真实微信/门店现场验收；现场验收清单见 `docs/operations/technician-body-map-field-acceptance.md`。

- 服务器部署根目录：`/root/hxy-diy-20260811`
- Release：`github-810817dd91d1-34032430331`
- 对应主干提交：`810817d`（画像迁移发布许可收口）
- Alembic head：`20260906_wellness_current`
- 数据库备份：发布前自动备份已校验（具体文件名见 GitHub 发布日志）
- 备份恢复演练：通过
- 公网 `/`、`/admin/`、`/technician/`、`/api/v1/health`：HTTP 200
- API 与数据库容器：运行中，发布验收时重启次数均为 0，数据库健康
- Release Manifest：发布脚本逐文件校验通过。

本次已按“恢复演练库先升级、生产库再升级”的顺序完成剩余前置迁移和当前画像迁移；发布脚本完成 Manifest、原子切换和三入口健康检查。

自动发布已通过专用受限密钥执行，复用当前 main CI 产物；纯文档变更保留静态门禁，不构建三端应用、不发布。发布脚本和说明见 `docs/customer-release-fast-path.md`。

## 三端当前状态

### 顾客端

- 正式入口：`https://diy.hexiaoyue.com/`
- 已发布评价显式评分、分层标签与隐私提示，评价不会阻断再次选购；项目详情支持安全分享，公开分享卡片带项目主图缩略图。真机微信分享卡片与弱网评价重试仍待现场验收。
- 已发布服务结束后重新进入空白选购、匿名多次提交整单门店价、浏览器匿名身份自愈、底部会员价和固定高度选购抽屉修复；匿名/非会员在选购清单中可逐项看到低于门店价的会员价参考。
- 已发布各类服务项目与茶饮详情介绍精简及排版优化，保留原 UI 和完整长图；价格标签紧贴金额，同价合并标注，有差价保留会员参考，局部单位不变。
- 新增发券策略已暂停：新客、主动领券、分享和邀请奖励不再发新券，匿名领券引导关闭；已领券与既有使用规则保留。生产 `coupon_issuance_enabled=False`，公开领券模板列表为空。
- 顾客端不直接管理技师服务参考标签；未来如增加顾客确认入口，必须复用后端版本化标签契约。
- 已发布单设备登录、“我的”任务中心、会员到期/累计节省、待评价和到店详情；会员本人权益采用唯一可信设备与30秒动态码，核验后才按会员价重算。
- 旧设备每5秒及页面重新可见/聚焦时校验登录代次；“我的”刷新保持在个人中心，不再返回项目列表。

### 管理端

- 正式入口：`https://diy.hexiaoyue.com/admin/`
- 管理端历史对 v5 身体服务记录只接收“下次服务：服务前再确认”安全摘要；服务端不下发身体部位、侧别、顾客自述或本次处理。
- 不得把结构化服务参考复制为普通用户标签，也不得用于分群、定价或自动营销。

### 技师端

- 正式入口：`https://diy.hexiaoyue.com/technician/`
- 快速服务参考已发布：重点部位、避让部位、力度、温度、服务反馈、下次建议、顾客确认和可选原话。
- 技师可读取当前顾客最近一次已确认的安全摘要；跨店、非活动服务和隐私字段受后端限制。
- 已发布当前画像底座：`customer_profile_current` 仅投影顾客已确认的 v3 安全字段，并按顾客与门店隔离；健康自述、原话和技师观察不进入当前画像投影。
- 已发布人体标注服务参考 v5：正反面点位、顾客自述情况、当前状态与本次处理，单次最多三项；仅本人完成服务的技师可写入。
- “查看上次服务参考”对身体记录仅显示“身体情况：服务前再确认”，不显示具体部位或自述。
- 已完成服务单不再显示只能在活动服务位读取的“查看上次服务参考”，避免前端触发服务端的状态保护 409；“填写服务参考”保持可用。
- 已发布服务连续性收敛：服务后快记默认只呈现服务方式、实际调整、反馈与下次安排；历史卡可展示上次实际调整。v5 本人历史支持一次保留原记录的带原因更正，仍受同门店、同顾客、同服务、同创建账号和幂等校验约束。

## 当前跨端契约

- 服务参考：新写入为 `schema_version=5`；v1–v4 只读兼容。
- 标签体系：新写入为 `taxonomy_version=service_reference_v4`。
- 后端保存稳定英文编码，中文文案仅用于展示。
- 顾客表达、技师观察、顾客确认和下次建议必须分开保存。
- 结构化服务参考与普通运营标签是两类数据，不得相互自动转换。

## 开始任务

先执行 `git fetch origin`，确认当前分支基于最新 `origin/main`，再读取 `docs/CONTEXT-MANIFEST.md` 和本端 workstream。涉及生产状态时必须重新实时核验。
