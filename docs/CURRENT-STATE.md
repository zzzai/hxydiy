# hxy-diy 当前状态

更新时间：2026-09-09 10:56（Asia/Shanghai）

本文件只记录三个开发窗口开始任务时需要知道的当前事实。发布和回滚历史保存在 `docs/WORK-STATUS.md`。

## 当前生产

- 主干已发布提交：`85c8f46410bb4705b1c60332772d934d9846a356`（PR #63，技师服务后极简快记）。GitHub 可信 CI `34304218505` 与生产部署 `34304347004` 均为 `success`，发布报告为 `deployment_succeeded`。
- 本次只调整技师端前端交互：默认快记收敛为沟通、关键调整、结果与一句交接；身体记录先选大部位，按需进入精确点位。没有数据库迁移、API 载荷、权限或隐私边界变化。
- 发布后只读公网核验：`/technician/`、`/api/v1/health` 均为 HTTP 200，线上入口的脚本含“交接一句话”和“先选常用部位”。未执行登录写入或门店真实服务；真机微信与营业现场验收仍待完成。
- 主干已发布提交：`f2f893797011aea1f9aa924edf2686c5b18af8f8`（PR #51，已完成服务单不再展示不适用的“查看上次服务参考”入口）。
- GitHub 可信 CI `34126497735` 与生产部署 `34126720373` 均为 `success`；授权测试技师账号在生产页面复验：已完成服务单仅显示“填写服务参考”，不再触发原先会返回 409 的读取请求。该复验不替代真实微信或门店现场验收。
- 本次只改技师端前端展示条件，不涉及数据库迁移、画像字段或服务端权限边界。
- 主干已发布提交：`4b2764331c86d9dae02452d748d5f08dc1c5b402`（PR #49，技师人体标注服务记录）。
- GitHub 可信 CI `34116640565` 与生产部署 `34116796271` 均为 `success`；发布报告状态为 `deployment_succeeded`。
- 本次没有数据库迁移；v1–v4 历史服务参考保持只读兼容。
- 以上是 CI/部署任务证据，不替代生产环境当前身份核验或真实微信/门店现场验收；现场验收清单见 `docs/operations/technician-body-map-field-acceptance.md`。

- 服务器部署根目录：`/root/hxy-diy-20260811`
- Release：`github-810817dd91d1-34032430331`
- 对应主干提交：`810817d`（画像迁移发布许可收口）
- Alembic head：`20260906_wellness_current`
- 数据库备份：发布前自动备份已校验（具体文件名见 GitHub 发布日志）
- 备份恢复演练：通过
- 公网 `/`、`/admin/`、`/technician/`、`/api/v1/health`：HTTP 200
- API 与数据库容器：运行中，发布验收时重启次数均为 0，数据库健康
- Release Manifest：发布脚本逐文件校验通过。

本次已按“恢复演练库先升级、生产库再升级”的顺序完成剩余前置迁移和当前画像迁移；发布脚本完成 Manifest、原子切换和三入口健康检查。

自动发布已通过专用受限密钥执行，复用当前 main CI 产物；纯文档变更保留静态门禁，不构建三端应用、不发布。发布脚本和说明见 `docs/customer-release-fast-path.md`。

## 三端当前状态

### 顾客端

- 正式入口：`https://diy.hexiaoyue.com/`
- 已发布服务结束后重新进入空白选购、匿名多次提交整单门店价、浏览器匿名身份自愈、底部会员价和固定高度选购抽屉修复；匿名/非会员在选购清单中可逐项看到低于门店价的会员价参考。
- 已发布各类服务项目与茶饮详情介绍精简及排版优化，保留原 UI 和完整长图；价格标签紧贴金额，同价合并标注，有差价保留会员参考，局部单位不变。
- 新增发券策略已暂停：新客、主动领券、分享和邀请奖励不再发新券，匿名领券引导关闭；已领券与既有使用规则保留。生产 `coupon_issuance_enabled=False`，公开领券模板列表为空。
- 顾客端不直接管理技师服务参考标签；未来如增加顾客确认入口，必须复用后端版本化标签契约。
- 已发布单设备登录、“我的”任务中心、会员到期/累计节省、待评价和到店详情；会员本人权益采用唯一可信设备与30秒动态码，核验后才按会员价重算。
- 旧设备每5秒及页面重新可见/聚焦时校验登录代次；“我的”刷新保持在个人中心，不再返回项目列表。

### 管理端

- 正式入口：`https://diy.hexiaoyue.com/admin/`
- 管理端历史对 v5 身体服务记录只接收“下次服务：服务前再确认”安全摘要；服务端不下发身体部位、侧别、顾客自述或本次处理。
- 不得把结构化服务参考复制为普通用户标签，也不得用于分群、定价或自动营销。

### 技师端

- 正式入口：`https://diy.hexiaoyue.com/technician/`
- 快速服务参考已发布：重点部位、避让部位、力度、温度、服务反馈、下次建议、顾客确认和可选原话。
- 技师可读取当前顾客最近一次已确认的安全摘要；跨店、非活动服务和隐私字段受后端限制。
- 已发布当前画像底座：`customer_profile_current` 仅投影顾客已确认的 v3 安全字段，并按顾客与门店隔离；健康自述、原话和技师观察不进入当前画像投影。
- 已发布人体标注服务参考 v5：正反面点位、顾客自述情况、当前状态与本次处理，单次最多三项；仅本人完成服务的技师可写入。
- “查看上次服务参考”对身体记录仅显示“身体情况：服务前再确认”，不显示具体部位或自述。
- 已完成服务单不再显示只能在活动服务位读取的“查看上次服务参考”，避免前端触发服务端的状态保护 409；“填写服务参考”保持可用。

## 当前跨端契约

- 服务参考：新写入为 `schema_version=5`；v1–v4 只读兼容。
- 标签体系：新写入为 `taxonomy_version=service_reference_v4`。
- 后端保存稳定英文编码，中文文案仅用于展示。
- 顾客表达、技师观察、顾客确认和下次建议必须分开保存。
- 结构化服务参考与普通运营标签是两类数据，不得相互自动转换。

## 开始任务

先执行 `git fetch origin`，确认当前分支基于最新 `origin/main`，再读取 `docs/CONTEXT-MANIFEST.md` 和本端 workstream。涉及生产状态时必须重新实时核验。
