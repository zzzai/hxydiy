# hxy-diy 共享记忆

本文件是顾客端、技师端和管理端协作窗口共同读取的项目记忆，只记录已经确认的事实、边界和决策，不复制聊天记录。

## 稳定业务边界

- 智慧宝负责真实开沙发、开房间、派钟、离位、清洁和物理资源释放。
- DIY 负责顾客电子菜单、选单、价格、提交、评价、技师画像记录和审计。
- DIY 普通服务位操作只保留“确认服务”和“服务结束”。
- 已生成智慧宝履约单的服务，不由 DIY 自动释放。
- 技师端不做派单、接单、认领、开房、离位、清洁或物理资源释放。
- 房间按一个独立单位展示，不单独记录房间内床位。

## 数据与权限原则

- Staff 负责登录、角色、门店绑定、账号状态和权限；Technician 负责技师档案、技能、等级、服务资格和提成信息，两者必须保持明确关联关系。
- 所有顾客数据、标签和服务记录按门店隔离，并遵循最小权限原则。
- 技师只能看到完成当前服务所必需的服务位、顾客选单和服务参考字段。
- 所有写操作必须服务端校验角色、门店范围、当前状态和幂等键，并写入审计。
- 顾客画像使用“顾客自述”“服务观察”“服务注意事项”等安全文案，避免医疗诊断式结论。
- 当前未绑定技师档案的旧 `staff` 账号会在认证层返回 `403 ROLE_MIGRATION_REQUIRED`，因此尚不能作为正式普通员工后台账号使用；该角色迁移缺口必须单独修复，不能用前端隐藏按钮替代。

## 当前生产事实

- 生产服务器：`root@115.190.245.14`。
- 生产部署根目录：`/root/hxy-diy-20260811`。
- 2026-08-31 只读核对的服务器 `current`：`technician-profile-contract-20260830-1`。
- 技师移动端入口：`https://diy.hexiaoyue.com/technician/`。
- 生产发布前必须完成测试、构建、数据库备份、Manifest 校验、原子切换和健康检查。

## 协作规则

- 三个窗口都先读取本文件、`PROJECT-CONTEXT-20260826.md` 和 `WORK-STATUS.md`。
- 每个窗口只修改自己负责的目录；公共文件由主窗口统一修改。
- 技师端窗口同步更新 `docs/workstreams/technician.md`；发布事实统一追加到 `docs/WORK-STATUS.md`。
- 不把浏览器 `localStorage` 当作跨端业务数据源；订单、服务状态和审计以 API/PostgreSQL 为准。
- 发现与本文件冲突的旧文档时，以服务器实际状态、最新测试结果和产品决策为准，并在工作状态中记录更正。
- 三端源码统一位于同一 GitHub monorepo：`admin-react`（管理端）、`diy-web`（顾客端）、`hxy-server`（后端）；各窗口通过任务分支和 Pull Request 协作，不直接推送 `main`。
- Obsidian 以本仓库根目录作为 Vault；共享记忆只通过 `docs/TEAM-MEMORY.md`、`docs/workstreams/*.md` 和 `docs/WORK-STATUS.md` 交换，禁止把聊天记录或本地缓存当作事实来源。

## 最近决策

| 日期 | 决策 | 影响范围 |
|---|---|---|
| 2026-09-01 | PR 不要求人工审批：采用 GitHub Ruleset 必需检查、默认分支可信 PR 门禁、AI 风险 Check Run 与精确 SHA 自动 squash 合并；生产 Environment 审批继续保留 | 全仓库协作、发布 |
| 2026-08-29 | 技师首页显示全部 8 个沙发和 7 个房间，并按状态使用不同颜色 | 技师端 |
| 2026-08-29 | 技师移动端保留三栏：今日服务、服务记录、我的 | 技师端 |
| 2026-08-29 | 服务结束后第一屏填写年龄段、性别、体型、职业场景和服务参考 | 技师端、后端 |
| 2026-08-30 | 技师确认/结束服务的幂等键必须绑定当前服务位、动作和登录技师；复用到其他目标时返回冲突，不得返回首笔结果 | 技师端、后端 |
| 2026-08-30 | 管理端媒体统一通过门店隔离上传 API 管理；店长仅本店，总部上传必须指定目标门店，图片对象软删除并审计 | 管理端、后端 |
| 2026-08-30 | 商品管理编辑统一使用 `PATCH /api/v1/admin/v2/products/{id}`；总部可修改商品主数据，绑定门店店长仅可修改 `publication_status`，旧 POST 更新路径继续兼容 | 管理端、后端 |
| 2026-08-30 | 商品店长上下架仅允许目标状态为 `published` 或 `inactive`；草稿/待发布可被门店首次上架，但 `archived` 为总部强制下线，门店不可恢复；商品列表支持服务端分页并兼容旧数组响应 | 管理端、后端 |
| 2026-08-30 | 登录员工快照保留未绑定门店 `admin` 的总部角色；绑定门店的历史 `admin` 对外呈现为 `manager`，前端据此显示总部目录编辑和门店选择能力 | 管理端、后端 |
| 2026-08-31 | 项目管理与商品遵循同一目录权限：总部可跨门店创建和编辑项目，店长仅可对本店项目切换 `published`/`inactive`；`archived` 是总部强制下线，店长不可恢复。项目列表必须由服务端执行分类、状态和分页筛选并返回准确总数，兼容旧数组响应 | 管理端、后端 |
| 2026-08-31 | 服务位二维码在管理端不受服务位即时占用状态限制：已授权后台角色可查看既有码并下载有效码；只有当前门店店长可停用、启用、重生或换绑。该配置不触发智慧宝物理资源操作，服务端继续强制校验门店归属和角色。 | 管理端、后端 |
| 2026-08-31 | 服务位“停用/重新启用”是独立于二维码的 DIY 配置开关：仅当前门店店长可对无活动占用的实际服务位操作；停用后拒绝新的扫码和共享 iPad 入口，保留二维码绑定以便恢复后继续使用。该开关不得触发智慧宝开关沙发、开房、离位、清洁或物理资源释放。 | 管理端、后端、顾客端契约 |
| 2026-08-31 | 服务位二维码重新生成只允许当前门店店长对“未被替换且服务位启用”的二维码操作；已替换二维码返回 `409 QR_REPLACED`，已停用服务位返回 `409 SERVICE_POSITION_DISABLED`。该限制避免重复二维码和不可投放的新码。 | 管理端、后端契约 |
| 2026-08-31 | `bound_qr` 是服务端在验证真实服务位二维码后写入的审计来源，不是顾客端可声明的入口来源；匿名入口伪造该值必须返回 `403 ENTRY_SOURCE_FORBIDDEN`，不得创建选单会话或服务位占用。 | 顾客端入口、后端安全契约 |
| 2026-08-31 | 可撤销的服务位二维码最低版本为 v2；生产环境必须拒绝不关联二维码记录的 v1 凭证（`403 QR_VERSION_EXPIRED`），以保证停用、重新生成和换绑可立即失效。v2 与新生成的 v3 保持兼容。 | 顾客端入口、管理端、后端安全契约 |
| 2026-09-01 | 房间配置页必须展示服务位 DIY 运营状态；店长仅可对无活动占用的本店服务位执行停用/重新启用，复用 `/api/v1/admin/service-positions/{room_id}/operational-status`；房间列表响应补充 `operational_status` 字段 | 管理端、管理端 API |
