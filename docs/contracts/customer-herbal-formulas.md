# 现煮草本泡五方选项

- 范围：`hxy-qiqing-30`。用户于 2026-09-14 确认门店已启用五方，并授权替换旧泡脚液选项。
- 新版本组编码 `footbath-formula`，免费偏好，必选且单选。顺序木、火、土、金、水；选项编码分别为 `formula-wood/fire/earth/metal/water`。
- 名称、状态说明、代表草本由目录 API 的 `name` / `description` 下发；前端只按编码映射五行标签与提示色，不按数组下标替换旧选项含义。提交沿用目录版本 ID 与选项 ID。
- 配置工具 `hxy-server/scripts/configure_herbal_formulas.py --store-id <id>` 默认只读，`--apply` 只复制已发布目录并准备新草稿；存在未发布草稿时拒绝覆盖。经现有目录发布流程验证后才切换当前版本。
- 旧发布目录、历史订单快照不修改；其他组的文案、价格、力度、局部多选规则不改。新草稿仅停用复制出的旧药方选项并新增五方，旧订单继续保留旧目录与选项。
- 顾客 UI 局部加强置于加购服务前，只交换展示位置；免费赠送项目暂不实施。
