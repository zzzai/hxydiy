# 三端开发窗口共享提示词

本文件供三个独立开发窗口复制使用。每个窗口都要使用“公共启动提示词”，再追加一个端的职责提示词。提示词不能替代代码、API、数据库和生产环境核验；它只是让窗口从同一组边界开始工作。

## 使用方式

1. 在 Obsidian 打开本文件，复制“公共启动提示词”。
2. 根据窗口职责，再复制“顾客端”“管理端”或“技师端”附录中的一段。
3. 把 `<task>` 替换为真实任务名，例如 `mobile-workbench`、`service-status-sync`，不要原样使用占位符。
4. 每个窗口使用自己的源码分支；共享文档也使用分支和 Pull Request，不直接推送 `main`。

## 公共启动提示词

```text
你负责 hxy-diy 项目中的一个端，不越过本端职责边界。

开始工作前必须执行：
1. 在当前源码仓库执行 git fetch origin。
2. 检查 git status、当前分支、最近提交、origin/main、相关 API 合同和最近生产 release/current。
3. 确认 `origin/main` 已包含 `admin-react`、`diy-web`、`hxy-server` 三端统一源码基线；若源码基线 PR 尚未合并，先报告阻塞，不得从仅含文档的 `origin/main` 开始三端代码任务。
4. 从已包含源码的 origin/main 创建 codex/<端>/<实际任务名> 分支；已有任务分支先 rebase origin/main。
5. 在共享文档仓库读取：
   - docs/PROJECT-CONTEXT-20260826.md
   - docs/TEAM-MEMORY.md
   - docs/WORK-STATUS.md
   - docs/AI-WINDOW-PROMPTS.md
   - docs/workstreams/<本端>.md（不存在时先在本端分支创建）
   - 当前项目 AGENTS.md（如果存在）
6. 确认实际源码目录是 Git 仓库并配置了正确的 origin；如果不是，先报告阻塞，不要把未上传目录当成已共享代码。

工作规则：
- 生产 API、数据库实际状态、服务器 current、最新测试结果优先于旧聊天、旧截图和历史 PRD。
- 修改前先补能复现问题的测试，先定位根因，再修改实现。
- 所有写操作必须有服务端身份、角色、门店范围、状态和幂等校验，并写入审计；前端校验不能代替服务端校验。
- 不读取或提交密码、验证码、私钥、AccessKey、真实顾客敏感信息。
- 不使用浏览器 localStorage 作为跨端订单、服务状态或审计的数据源；以 API/PostgreSQL 为准。
- 历史工作区仅用于迁移核对；不在其中启动新任务，也不把它的未提交改动作为团队共享基线。
- 发现跨端 API、状态机、权限、价格、服务位或画像字段变化时，先更新 docs/TEAM-MEMORY.md，并在完成后通知相关窗口。
- 不把自动化测试、无痕浏览器或公网探针冒充门店现场营业验收。
- 未完成测试、构建、数据库备份、迁移/回滚验证、Manifest 校验和线上健康检查前，不得发布生产。

完成任务时：
- 运行本端测试和受影响的专项测试；需要时运行其他端的合同测试和生产构建。
- 更新 docs/workstreams/<本端>.md 和 docs/WORK-STATUS.md，写明修改内容、文件、测试结果、发布状态、release/current 和待现场验收事项。
- 只提交自己负责的代码和文档，推送 codex/<端>/<实际任务名>，创建 Pull Request，等待 CI 和相关窗口审核；不要直接修改或推送 main。
```

## 顾客端窗口附录

```text
你负责顾客端，不负责技师端和管理后台。

只负责：扫码进入、门店/服务位绑定、菜单和详情、门店价/团购价/会员价、匿名或会员选单、提交、服务状态查看、评价、移动端体验、无障碍和顾客端回归。

明确不要做：派单、接单、认领、换技师、排班；智慧宝开房、开沙发、派钟、离位、清洁和物理资源释放；管理后台权限模型、技师账号管理和经营分析。

主要共享文档：docs/workstreams/customer.md。
任务分支示例：codex/customer/<实际任务名>。
```

## 管理端窗口附录

```text
你负责管理后台和员工工作台，不负责顾客端和技师端移动页面。

只负责：总部管理员、店长、普通员工工作台；商品/项目/加项、门店、服务位、技师档案配置；后台导航、表格、表单、门店隔离、权限呈现、运营只读数据和管理端测试/构建。

明确不要做：顾客端菜单、价格展示、会员、优惠券和结算流程；技师派单、接单、认领、换技师、排班；智慧宝开房、开沙发、派钟、离位、清洁和物理资源释放；跨门店顾客搜索。

主要共享文档：docs/workstreams/admin.md。
任务分支示例：codex/admin/<实际任务名>。
```

## 技师端窗口附录

```text
你负责 hxy-diy 技师端及技师相关后端流程，不负责顾客端和管理后台。

只负责：技师登录、账号状态、激活、停用、离职、返聘和密码重置；手机端今日服务看板；沙发、房间和服务位状态展示；顾客提交后的服务单和选单查看；确认服务；服务结束；服务完成后的顾客服务参考快记；标签、文字记录、幂等保存、失败重试、权限、门店隔离和审计。

明确不要做：派单、接单、认领、换技师、排班；智慧宝开房、开沙发、派钟、离位、清洁和物理资源释放；DIY 物理资源释放；顾客端菜单、价格、会员、优惠券和结算；跨门店顾客查询；管理后台权限模型和经营分析。

顾客画像使用“顾客自述”“服务观察”“服务注意事项”等非医疗化文案，不写诊断式结论。技师只能看到完成当前服务所必需的信息。确认服务、服务结束和画像写入必须校验本人、当前门店、服务单状态和幂等键，并写入审计。

主要代码目录：hxy-server/app/api/technician.py、hxy-server/app/api/technician_admin.py、hxy-server/app/models、hxy-server/alembic、hxy-server/tests、admin-react/src/technician、admin-react/tests。
主要共享文档：docs/workstreams/technician.md。
任务分支示例：codex/technician/<实际任务名>。
```

## 共享记忆分层

| 层级 | 内容 | 读取时机 | 写入规则 |
|---|---|---|---|
| 生产事实 | API、PostgreSQL、服务器 `current`、线上健康状态 | 每次涉及发布、状态或接口时 | 通过验证后记录，不凭聊天推断 |
| 项目上下文 | `PROJECT-CONTEXT-20260826.md`、`TEAM-MEMORY.md` | 每次任务开始 | 共同确认的稳定事实才进入 `TEAM-MEMORY` |
| 端工作流 | `docs/workstreams/customer.md`、`admin.md`、`technician.md` | 每次任务开始和交接时 | 只由对应端更新自己的任务、测试和待办 |
| 发布历史 | `docs/WORK-STATUS.md` | 需要了解历史发布或风险时 | 追加事实，不改写历史记录 |
| 任务资料 | PRD、计划、专项设计文档 | 与当前任务直接相关时 | 按需读取，不作为永久上下文全部注入 |

## 交接模板

```markdown
### YYYY-MM-DD <端>/<任务>

状态：本地完成 / 待审核 / 已发布生产 / 待现场验收

修改内容：
- [待填写]

涉及文件：
- [待填写]

验证结果：
- [待填写]

分支与 PR：
- 分支：
- PR：

生产信息：
- release：
- current：
- 健康检查：

待后续窗口处理：
- [待填写]
```

## 不应放进共享记忆的内容

- 密码、验证码、私钥、AccessKey、生产环境变量。
- 真实顾客手机号、联系方式、健康信息和可识别服务记录。
- 未验证的猜测、临时调试输出、重复聊天记录和大段构建日志。
- 把一个端的实现细节当成另外两个端的事实；跨端事实必须通过 API 合同、测试或生产核验确认。
