# 技师端工作流

## 负责范围

- 技师登录、首次激活、密码重置、停用、离职和返聘。
- 手机优先的技师今日服务看板、服务记录和个人状态。
- 沙发、房间区位展示；当前门店 8 个沙发、7 个房间，房间不拆分床位。
- 顾客提交后的服务单查看、项目摘要和局部项目部位展示。
- 仅执行“确认服务”和“服务结束”。
- 服务完成后的顾客服务参考快记、幂等写入和审计。
- Staff/Technician 关联、技师角色权限、门店隔离和审计。

## 明确不做

- 派单、接单、认领、换技师或排班。
- 智慧宝开房、开沙发、派钟、离位、清洁和物理资源释放。
- 跨门店顾客搜索、顾客联系方式和无关经营数据。
- 结算、改价、会员操作和经营分析。

## 主要代码目录

- `hxy-server/app/api/technician.py`
- `hxy-server/app/api/technician_admin.py`
- `hxy-server/app/models`
- `hxy-server/alembic`
- `hxy-server/tests`
- `admin-react/src/technician`
- `admin-react/tests`

## 当前已确认状态

- 技师移动端生产入口为 `/technician/`，不进入桌面管理后台。
- 今日页按“沙发”“房间”分组展示全部服务位；空闲、待服务、服务中、已完成使用不同状态色。
- 点击有订单的区位可查看服务单和顾客选单；服务完成后可打开服务参考快记。
- 技师服务参考写入已具备本人完成服务关联、门店校验、字段白名单、医疗诊断词拦截、幂等和审计。
- 已完成线上手机验收：390x844 视口可查看 8 个沙发、7 个房间，点击 3 号沙发可查看服务单 #325；页面底部导航未遮挡最后一个房间卡片。

## 开始任务前检查

1. 读取 `docs/PROJECT-CONTEXT-20260826.md`、`docs/WORK-STATUS.md`、`docs/TEAM-MEMORY.md`。
2. 读取本文件和当前项目 `AGENTS.md`（如果存在）。
3. 检查当前源码、未提交变更、最近生产 release 和服务器实际 `current`。
4. 先补失败测试，再修改实现。
5. 涉及生产时执行数据库备份、Manifest 校验、健康检查和关键链路验收。

## 完成任务后记录

在本文件追加：

- 修改内容
- 涉及文件
- 测试结果
- 是否发布及生产 release
- 风险和待门店现场验收事项

同时更新 `docs/WORK-STATUS.md`，不得只在聊天中报告结果。

## 2026-08-30 源码基线发布就绪性复核（未发布）

### 本地完成

- 在隔离工作树复核候选源码基线 `origin/codex/admin/monorepo-bootstrap` 的提交 `59cafdc`；已确认其中包含技师服务确认/结束幂等键按“服务位、动作、登录技师”作用域校验及对应回归测试。
- 技师后端专项通过：`30 passed`，覆盖服务位范围、账号生命周期、服务确认/结束、画像快记及幂等契约。
- 管理端完整测试通过：`107 passed`；生产构建通过。构建仍有既有的单个共享包体积警告。

### 发布阻断

- 候选基线全量后端测试结果为 `516 passed, 14 failed, 6 skipped`，因此不能创建可合并的源码基线 PR，更不能发布生产。
- 其中 10 个发布契约失败的直接原因是候选基线未包含生产必需的 `deploy/` 目录和 `tmp-codex/publish_body_part_catalogs.py`；发布脚本、Manifest 与健康检查无法在该基线上复核。
- 另有 4 个失败的测试夹具仍创建未绑定的旧 `staff` 角色，而现行迁移和认证契约明确拒绝该角色。不得通过放宽认证来使测试变绿；需要由对应业务负责人将夹具迁移为正式角色，或明确该操作的正式权限模型。

### 发布状态与待办

- 未发布生产，未修改服务器、数据库、生产 release 或 `current`。
- 技师功能本身仅完成自动化验证，仍需在已补齐发布资产且全量测试通过的源码基线上，完成测试账号的顾客提交、看板同步、确认服务、服务结束、快记重试和审计现场闭环。
